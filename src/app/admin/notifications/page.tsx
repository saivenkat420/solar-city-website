'use client';

import Layout from '../../../components/Layout';
import SupabaseRealtimeNotifications from '../../../components/SupabaseRealtimeNotifications';

export default function NotificationsPage() {
  return (
    <Layout>
      <SupabaseRealtimeNotifications />
    </Layout>
  );
} 